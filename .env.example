# Postgres Database Configuration
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
# if you do not require any certificate, you can ignore this
# if you want to need ssl connection with the DB, place the certificate inside the certificates folder then give the download link of the certificate
DB_CERTIFICATE=

# Github Token for increasing rate limit of reading the repository
GITHUB_TOKEN=

# LLM_PROVIDER=deepseek | google (for google ai studio)
LLM_PROVIDER=
LLM_APIKEY=
LLM_MODELNAME=