# Lambda API Handler Dependencies
# Minimal dependencies for AWS Lambda deployment

# AWS SDK


# HTTP client for async operations (used by shared modules)
aiohttp>=3.9.0

# Type hints support (included in Python 3.11+ but needed for older versions)
typing-extensions>=4.9.0
